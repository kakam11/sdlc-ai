"""Generate a self-contained HTML slide presentation from parsed slides.

Implemented in later tasks: resolve_image() (image -> base64 data URI) and
generate_html() (slides -> single HTML document with inline CSS/JS).
"""
